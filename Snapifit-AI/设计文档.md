# Snapifit-AI 设计文档

## 1. 概述

### 1.1 项目背景
Snapifit-AI 是一个综合性的健康管理和AI辅助系统，旨在通过先进的人工智能技术为用户提供个性化的健康建议、营养指导、运动计划和心理健康支持。系统整合了食物记录、运动追踪、健康数据分析、AI聊天机器人等多种功能，为用户提供全方位的健康管理解决方案。

### 1.2 系统定位
- **健康管理平台**：提供全面的健康数据记录和分析
- **AI辅助系统**：通过AI技术提供个性化建议和指导
- **用户交互中心**：建立用户与AI之间的智能对话系统
- **数据驱动平台**：基于用户数据提供精准的健康建议

### 1.3 核心价值
- **个性化**：根据用户历史数据提供定制化建议
- **智能化**：利用AI技术优化健康管理流程
- **全面性**：覆盖营养、运动、心理等多个健康领域
- **易用性**：简洁直观的用户界面和交互体验

## 2. 架构设计

### 2.1 整体架构
```
┌───────────────────────────────────────────────────────────────────────────────┐
│                         Snapifit-AI 核心模块划分视图                          │
├───────────────────────────────────────────────────────────────────────────────┤
│ 1. 前端模块 (表示层)                                                          │
│    ├── 用户界面模块      ▹ 响应式UI渲染 / 多角色聊天视图 / 健康数据图表看板   │
│    ├── 交互逻辑模块      ▹ 拍照上传预处理 / 表单数据实时校验 / 路由权限守卫   │
│    └── 状态管理模块      ▹ 全局状态共享 / 本地健康数据持久化缓存 / 会话控制   │
│                                                                               │
│ 2. 后端模块 (业务逻辑层)                                                      │
│    ├── 用户管理模块      ▹ JWT 无状态鉴权 / 账号生命周期维护 / 用户偏好配置   │
│    ├── 健康数据管理模块  ▹ 多模态热量计算 / 运动消耗统筹 / 趋势周期统计分析   │
│    ├── 专家对话模块      ▹ 多 Agent 角色分发 / SSE 流式响应 / 动态提示词组装  │
│    ├── 智能建议模块      ▹ 基于数据缺口匹配 / 个性化建议定时推送 / 效果反馈   │
│    ├── AI 记忆管理模块   ▹ 核心上下文提取聚合 / 短期会话维系 / 长期事实沉淀   │
│    └── 数据库管理模块    ▹ ORM 映射处理 / HBR-Cache 高速缓存调度 / 事务控制   │
│                                                                               │
│ 3. 数据层 (持久化层)                                                          │
│    ├── 用户数据表        ▹ 账号基准信息 / 静态生理指标 / 个性化健康与减脂目标 │
│    ├── 健康记录表        ▹ 食物图片识别日志 / 热量摄入消耗明细 / 运动打卡时序 │
│    ├── 专家对话表        ▹ 全局会话 ID 追踪 / AI 角色定义 / 多轮聊天上下文流  │
│    ├── 建议历史表        ▹ 已生成健康建议留存 / 用户采纳状态 / 建议有效性评分 │
│    └── AI 记忆表         ▹ 动态提取的用户画像标签 / 饮食偏好特征 / 长期沉淀   │
└───────────────────────────────────────────────────────────────────────────────┘
```
### 2.2 技术栈
1. 核心框架
全栈框架：Next.js 15.2.4 (基于 App Router 架构，统一前后端路由与渲染)
开发语言：TypeScript + Node.js (提供全链路的类型安全)
2. 前端 
样式解决方案：Tailwind CS
UI 基础组件库：Radix UI (无头组件库 Headless UI，提供极佳的无障碍访问和自定义能力)
国际化方案：next-intl 
3. 逻辑与状态管理 
全局状态管理：React Context API (轻量级，替代 Redux 处理全局状态)
表单处理：React Hook Form (高性能表单状态管理与校验)
4. 数据与通信层
数据库：SQLite (轻量级本地文件数据库，极大地降低了部署和运维成本)
接口规范：HTTP / RESTful API (配合 Next.js 的 Route Handlers)
数据交换格式：JSON

5. 工程化
构建工具：Next.js 内置 Webpack/Turbopack 构建系统

部署架构：全栈一体化部署 (单体应用模式，极大概率会托管在 Vercel 或通过 Docker 容器化部署)
开发环境：VS Code

### 2.3 模块划分
```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       Snapifit-AI 功能模块与业务边界划分                    │
├───────────────┬──────────────────────┬──────────────────────────────────────┤
│   所属分层    │     核心功能模块     │          业务边界与代码职责          │
├───────────────┼──────────────────────┼──────────────────────────────────────┤
│               │ 界面呈现 (UI)        │ 响应式页面渲染、健康数据可视化、组件 │
│ 1. 前端模块   │ 交互逻辑 (Hooks)     │ 图片上传处理、表单实时校验、路由守卫 │
│               │ 状态管理 (Store)     │ 全局状态同步、本地健康缓存、主题控制 │
├───────────────┼──────────────────────┼──────────────────────────────────────┤
│               │ 核心业务 (Core)      │ 账号鉴权管理、热量计算引擎、趋势分析 │
│ 2. 后端模块   │ AI 引擎 (AI Service) │ 多Agent角色路由、SSE流式输出、提示词 │
│               │ 基础设施 (Infra)     │ HBR-Cache调度控制、拦截器、ORM映射   │
├───────────────┼──────────────────────┼──────────────────────────────────────┤
│               │ 业务资产 (Business)  │ 用户基准信息、多模态饮食与运动日志表 │
│ 3. 数据模块   │ 会话数据 (Session)   │ 全局聊天上下文追踪、历史消息流留存   │
│               │ 记忆特征 (Memory)    │ 动态用户健康画像、饮食偏好、长期沉淀 │
└───────────────┴──────────────────────┴──────────────────────────────────────┘
```
### 2.4 整体设计

#### 系统架构模式
```
┌─────────────────────────────────────────────────────────────────────────────┐
│                 Snapifit-AI 核心分层架构设计 (Next.js 全栈)                 │
├─────────────────────────────────────────────────────────────────────────────┤
│  [表示层] Client Presentation Layer (Next.js Client Components)             │
│   ├─ 视图渲染 : Radix UI 组件库 / Tailwind CSS / 响应式多模态视图         │
│   ├─ 状态交互 : React Context 全局状态管理 / React Hook Form 表单流转     │
│   └─ 业务感知 : 拍照上传界面 / AI 实时对话流展示 (SSE 接收端)             │
├─────────────────────────────────────────────────────────────────────────────┤
│  [应用与网关层] Application & API Layer (Next.js App Router)                │
│   ├─ 路由控制 : Server Components 服务端渲染 / API Routes 接口分发        │
│   ├─ 安全鉴权 : JWT Token 校验 / 路由守卫 (Middleware 拦截器)             │
│   └─ 协议转换 : RESTful 请求处理 / AI 聊天 WebSocket & SSE 长连接维持     │
├─────────────────────────────────────────────────────────────────────────────┤
│  [领域驱动层] Domain Business Layer (Node.js & AI Engine)                   │
│   ├─ 健康核心 : 食物多模态热量解析 / 运动消耗算法 / 趋势周期统筹统计算法│
│   └─ AI 引擎  : Prompt 提示词工程管理 / 多 Agent 专家调度 / RAG 记忆检索│
├─────────────────────────────────────────────────────────────────────────────┤
│  [数据与底座层] Data & Infrastructure Layer                                 │
│   ├─ 缓存加速 : 调度池 (拦截高频健康数据读写，降低磁盘 I/O)     │
│   ├─ 数据映射 : 轻量级 ORM (如 Prisma/Drizzle) 提供类型安全的数据库操作 │
│   └─ 本地持久 : SQLite (结构化业务主库) + 本地特征库 (AI 画像与特征向量)│
└─────────────────────────────────────────────────────────────────────────────┘
```
#### 系统交互设计

##### 数据流设计
系统的数据流转严格遵循单向数据流（Unidirectional Data Flow）与读写分离原则。考虑到系统包含高频的 UI 交互和耗时的 AI 推理，数据流设计按业务场景拆分为三种核心流转模式：

**前端状态数据流 (Client State Flow)**
在 Next.js 的客户端组件（Client Components）中，严格遵循 React 的单向数据流闭环：
* **Action 触发**：用户操作（如点击拍照上传食物或提交当前体重）触发前端事件处理器。
* **数据上报**：前端通过 Next.js Server Actions 或 Fetch API 将 Payload 发送至应用网关层，在此层完成 JWT 鉴权与 API 防刷限流。
* **State 更新**：收到后端领域业务层的成功响应后，将最新健康数据派发（Dispatch）给 React Context 全局状态。
* **UI 重绘**：状态变更驱动 Radix UI 组件库重新渲染（如更新健康看板），确保界面的数据源唯一性（Single Source of Truth）。

**AI 专家多轮对话与推理流 (AI RAG & Streaming Flow)**
由于大模型推理通常存在较高延迟，传统的 HTTP Request-Response 模式会导致前端长时间白屏阻塞，因此 AI 数据流采用异步流式分块传输机制：
* **意图分发**：用户发送文字或图片后，后端 AI 引擎层首先进行 Agent 路由判断（判定由营养专家、健身专家还是综合心理专家接管）。
* **长期记忆检索 (RAG)**：引擎层向底层的本地特征库发起检索，提取该用户的体脂基准、饮食偏好、历史病史等记忆特征。
* **提示词组装**：将检索到的背景上下文与当前用户的输入（如当前的热量缺口）进行动态合并，生成完整的 Prompt 提交给大模型。
* **SSE 流式输出**：Node.js 后端在调用 AI 接口时开启 `stream: true` 模式。无需等待模型全部生成完毕，接收到首个 Token 即刻通过 Server-Sent Events (SSE) 长连接向前端持续推送数据块。前端渲染出“打字机”效果，极大降低用户的感知延迟。

**缓存与持久化数据流 (Cache & Persistence Flow)**
为保护底层 SQLite 数据库免受高频零碎数据（如连续打卡状态更新）的并发 I/O 冲击，系统引入旁路缓存（Cache-Aside）与穿透写入（Write-Through）的混合策略：
* **读高频流**：应用层发起读取请求优先查询内存缓存池，若命中直接返回；未命中则穿透查询 SQLite 主库，将结果回写缓存后返回前端。
* **写聚合流**：非核心时序健康日志先快速写入缓存池，由后端定时任务批量持久化到 SQLite；核心配置数据（如修改密码、重置目标）则采用同步机制直接穿透落盘，确保强一致性。

---

#### 数据库设计

针对 SQLite 的轻量级特性及 AI 系统的复杂性，设计中大量引入了 `JSON` 数据类型来处理非结构化健康档案，合并了散落的动态表，并新增了专门用于 RAG 检索的特征记忆表。

##### 表结构设计

**用户核心档案表（user_profiles）**
*描述：存储用户基础信息，复杂文本类型统一为 JSON 以利于 AI 解析和前端双向绑定。*
| 字段名 | 类型 | 描述 |
|--------|------|------|
| user_id | VARCHAR(50) | 用户 ID，主键 |
| basic_metrics | JSON | 基础生理指标集合（含体重、身高、年龄、性别等） |
| health_goals | JSON | 健康目标集合（含目标类型、目标体重、卡路里等） |
| bmr_settings | JSON | 基础代谢设置（含计算公式、依据、体脂率等） |
| medical_history | JSON | 用户病史与过敏史（结构化数组） |
| lifestyle_tags | JSON | 生活方式标签（如：久坐、熬夜等） |
| professional_mode | BOOLEAN | 是否开启专业模式 |
| created_at | DATETIME | 账号创建时间 |
| updated_at | DATETIME | 资料最后更新时间 |

**健康时序指标表（health_metrics_log）**
*描述：将原有的体重记录与活动水平记录合并，方便后续水平扩展心率、睡眠等更多维度。*
| 字段名 | 类型 | 描述 |
|--------|------|------|
| log_id | VARCHAR(50) | 记录 ID，主键 |
| user_id | VARCHAR(50) | 用户 ID，外键索引 |
| metric_type | VARCHAR(20) | 指标类型（weight / activity_level） |
| metric_value | DOUBLE | 指标数值 |
| timestamp | DATETIME | 记录时间 |

**多模态食物记录表（food_entries）**
*描述：增加图片路径与 AI 视觉置信度，支撑核心的拍照打卡业务。*
| 字段名 | 类型 | 描述 |
|--------|------|------|
| log_id | VARCHAR(50) | 记录 ID，主键 |
| user_id | VARCHAR(50) | 用户 ID，外键 |
| image_url | VARCHAR(255) | 食物图片存储路径 |
| food_name | VARCHAR(100) | 识别出的食物名称 |
| consumed_grams | INT | 摄入克数 |
| meal_type | VARCHAR(20) | 餐别（早、中、晚、加餐） |
| macros_json | JSON | 宏量营养素明细（碳水、蛋白质、脂肪等） |
| total_calories | INT | 摄入总卡路里 |
| ai_confidence | DOUBLE | AI 视觉识别置信度 (0.0 - 1.0) |
| is_estimated | BOOLEAN | 是否为估算值 |
| timestamp | DATETIME | 记录时间 |

**运动消耗记录表（exercise_entries）**
| 字段名 | 类型 | 描述 |
|--------|------|------|
| log_id | VARCHAR(50) | 记录 ID，主键 |
| user_id | VARCHAR(50) | 用户 ID，外键 |
| exercise_name | VARCHAR(100) | 运动名称 |
| exercise_type | VARCHAR(20) | 运动类型 |
| metrics_json | JSON | 复合运动指标（时长、距离、组数、次数、重量等） |
| estimated_mets | INT | 估算的代谢当量 |
| calories_burned | INT | 估算的卡路里消耗 |
| muscle_groups | JSON | 锻炼的肌肉群 |
| is_estimated | BOOLEAN | 是否为估算值 |
| timestamp | DATETIME | 记录时间 |

**统一大模型对话表（ai_chat_messages）**
*描述：废弃动态建表反模式，将会话合并，通过角色字段区分，并增加 Token 消耗审计。*
| 字段名 | 类型 | 描述 |
|--------|------|------|
| message_id | VARCHAR(50) | 消息 ID，主键 |
| session_id | VARCHAR(50) | 会话上下文 ID，外键 |
| user_id | VARCHAR(50) | 用户 ID，外键 |
| expert_role | VARCHAR(20) | 接管专家角色（nutritionist/trainer/psychologist） |
| role | VARCHAR(10) | 角色（user/assistant/system） |
| content | TEXT | 消息主体内容 |
| reasoning_content | TEXT | 思考过程内容（CoT 日志） |
| token_count | INT | 消耗的 Token 数量 |
| timestamp | DATETIME | 消息时间 |

**AI 长期记忆特征库（ai_long_term_memories）**
*描述：支撑 RAG 检索的核心表，用于沉淀大模型从聊天中提取的用户习惯与事实。*
| 字段名 | 类型 | 描述 |
|--------|------|------|
| memory_id | VARCHAR(50) | 记忆 ID，主键 |
| user_id | VARCHAR(50) | 用户 ID，外键 |
| category | VARCHAR(20) | 记忆分类（diet / injury / habit） |
| fact_content | TEXT | 提取的客观事实描述 |
| source_message_id | VARCHAR(50) | 来源消息 ID，用于记忆溯源 |
| created_at | DATETIME | 特征提取时间 |

**智能建议历史表（suggestion_history）**
| 字段名 | 类型 | 描述 |
|--------|------|------|
| history_id | VARCHAR(50) | 历史记录 ID，主键 |
| user_id | VARCHAR(50) | 用户 ID，外键 |
| suggestion_data | JSON | 结构化的建议数据 |
| status | VARCHAR(20) | 建议状态（viewed/adopted） |
| effectiveness_score | INT | 用户效果评分 |
| generated_at | TIMESTAMP | 生成时间 |
| viewed_at | TIMESTAMP | 查看时间 |
| adopted_at | TIMESTAMP | 采纳时间 |

##### 数据关系设计
* **核心挂载点**：所有业务流水表（健康时序指标表、食物记录表、运动记录表、大模型对话表、长期记忆库、建议历史表）均通过 `user_id` 与 **用户核心档案表（user_profiles）** 形成一对多的强关联约束。
* **上下文闭环**：**统一大模型对话表（ai_chat_messages）** 内部通过 `session_id` 串联出完整的单次多轮对话上下文；同时，**AI 长期记忆特征库（ai_long_term_memories）** 通过 `source_message_id` 字段反向关联至对话表，实现 AI 记忆生成的可解释性与溯源。
* **非结构化扩展**：利用 SQLite 对 JSON 的原生支持，系统可在不频繁执行数据库表结构变更（Migration）的情况下，动态向各业务表的 JSON 字段中追加新的标签与指标数据，提升后续功能的迭代速度。

## 3. 功能设计

### 3.3 具体 ui 设计
#### 3.3.1 登录与注册视图 (Auth View)
结构：全屏居中布局。主视觉为一个包含毛玻璃效果（Glassmorphism）的白板卡片。

组件映射：

顶部使用 Radix Tabs 组件实现“登录/注册”无刷新切换。

密码框包含内置的 SVG 眼睛图标，点击切换 type="password" 与 type="text"。

错误反馈：使用 React Hook Form 捕获错误，触发输入框 Tailwind 类名切换（如 border-red-500），并在底部弹出全局 Toast。

#### 3.3.2 健康主工作台 (Dashboard View)
结构：移动端优先的流式垂直布局。

组件映射：

头部区：展示用户头像与当日问候语。

核心指标卡（Cards）：利用 SVG 绘制的环形进度条（Progress Ring），中心显示大字号的剩余卡路里数值。

快捷操作栏（FAB - Floating Action Button）：固定在屏幕底部的悬浮按钮组，包含核心的“拍照（Snap）”和“手动记录”主功能入口，点击具有轻微的缩放反馈（active:scale-95）。

#### 3.3.3 拍照识图视图 (Snap View)
结构：沉浸式多模态交互界面。

组件映射：

相机图层：全屏覆盖的视频流容器，中心带有半透明的对焦框提示。

加载反馈态（Skeleton & Loader）：在等待后端多模态 API 响应时，界面覆盖骨架屏，并伴有自上而下的扫描线动画（通过 Tailwind animate-pulse 及自定义 keyframes 实现）。

结果抽屉（Bottom Sheet）：识别成功后，使用 Radix Dialog 模拟从底部弹出的抽屉面板，以列表形态展示识别到的食物项，支持左滑删除或点击修改克数。

#### 3.3.4 AI 专家对话室 (Agent Chat View)
结构：典型的 IM（即时通讯）全屏气泡布局，底部固定输入框。

组件映射：

专家切换栏：页面顶部的横向滚动胶囊按钮（Pills），切换不同的 AI 角色（如营养师、教练）。

消息气泡：根据数据源区分样式。用户气泡靠右对齐（品牌色底色），AI 气泡靠左对齐（浅灰/暗色底色）。

流式打字机交互：接收到 SSE 数据流时，通过 React 状态逐字更新文本内容。末尾动态渲染一个闪烁的光标（animate-blink），增强“AI 正在思考和打字”的陪伴感。
工作周报
姓名：李子林
组号：7
周期：2026年4月23日 — 2026年4月29日

| 维度 | 详细内容 |
| :--- | :--- |
| **一、本周工作完成情况** | **1. 数据库选型重大调整**：从原 MySQL 主从方案切换为 SQLite 轻量级本地数据库，大幅降低部署运维成本，进度100%。<br>**2. 七张核心表结构设计**：完成 `user_profiles`、`health_metrics_log`、`food_entries`、`exercise_entries`、`ai_chat_messages`、`ai_long_term_memories`、`suggestion_history` 全部字段定义。<br>**3. 数据关系与扩展策略**：设计所有业务表通过 user_id 与用户表的一对多强关联，并利用 SQLite JSON 原生支持实现非结构化扩展。<br>**4. 三种数据流模式定义**：输出前端状态数据流、AI RAG & Streaming 流、缓存与持久化流的完整设计。 |
| **二、本周重点成果/亮点** | 核心产出：2.6 数据库设计全节（含选型/表结构/关系设计）、2.7 系统交互设计数据流部分。<br>**解决问题**：废弃了原来的动态建表反模式（chat_{expert_role}），统一为 ai_chat_messages`单表通过 expert_role 字段区分，消除 Schema 膨胀风险。<br>**数据/效果**：旁路缓存+穿透写入混合策略预计可降低 SQLite 磁盘 I/O 约60%。 |
| **三、遇到的问题与需要支持** | 问题：SQLite 在高并发写入场景（如多用户同时打卡）下可能触发数据库锁。<br>需支持：需评估实际并发量级，确认是否需要 WAL 模式优化。 |
| **四、下周工作计划** | 计划 ：使用 Drizzle ORM 初始化 SQLite 数据库，编写全部 7 张表的 Schema Migration 脚本。 |
| **五、备注/其他** | 无 |
